var Clock = "12h";  // choose between "12h" or "24h"
var MonthType = "short"; // Choose between "short" and "long" e.g Aug and August
var WeekdayType = "short"; // Choose between "short" and "long" e.g Thu and Thursday
var FGColor = "rgba(0,0,0)"; // Foreground color for all text
var ShadowAmount = "3"; // how much of a shadow should be on the text, in px

